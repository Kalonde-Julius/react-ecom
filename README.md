This is a Full stack web app using React.js for the Frontend and Laravel for the Backend.

To run the Frontend (React) via cmd :
- cd frontend
- npm install
- npm run dev

To run the Backend (Laravel) via cmd :
- cd backend
- composer update
- npm install
- composer run dev
